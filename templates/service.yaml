apiVersion: v1
kind: Service
metadata:
  name: {{ template "argo-app.fullname" . }}
  labels:
    app: {{ template "argo-app.name" . }}
    chart: {{ template "argo-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "argo-app.name" . }}
    release: {{ .Release.Name }}
